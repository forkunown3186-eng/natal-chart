{
  "name": "natal-chart",
  "version": "2.0.0",
  "description": "High-precision natal astrology chart calculator",
  "private": true,
  "scripts": {
    "build":   "echo 'Static app — no build step needed'",
    "android": "npx cap sync android && npx cap open android",
    "apk":     "npx cap sync android && cd android && ./gradlew assembleDebug",
    "apk:release": "npx cap sync android && cd android && ./gradlew assembleRelease",
    "sync":    "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core":    "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
